workflows:
  android-build:
    name: Android Build
    environment:
      vars:
        APPLICATION_ID: "com.yourpackage.app"  
    scripts:
      - name: Build APK
        script: |
          ./gradlew assembleDebug
    artifacts:
      - app/build/outputs/apk/debug/app-debug.apk